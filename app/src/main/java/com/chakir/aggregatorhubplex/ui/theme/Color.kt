package com.chakir.aggregatorhubplex.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETTE DE BASE ---
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// --- COULEURS DE MARQUE ---
/** Orange emblématique de Plex */
val PlexOrange = Color(0xFFE5A00D) // L'accent principal
/** Orange plus sombre pour les dégradés ou les états pressés */
val PlexOrangeDim = Color(0xFFCC8E0B)

// --- COULEURS DE FOND (Dark UI) ---
/** Noir très profond, optimisé pour les écrans OLED des TV. */
val AppBackground = Color(0xFF0F0F0F)
/** Gris foncé pour les surfaces (cartes, headers, dialogues). */
val SurfaceColor = Color(0xFF1F1F1F)
/** Gris légèrement plus clair pour indiquer le focus au clavier/télécommande. */
val SurfaceHighlight = Color(0xFF2A2A2A)

// --- TEXTE ---
/** Blanc cassé pour le texte principal (moins agressif pour les yeux dans le noir). */
val TextPrimary = Color(0xFFE5E5E5)
/** Gris clair pour les sous-titres et métadonnées. */
val TextSecondary = Color(0xFFB3B3B3)
/** Gris foncé pour les textes désactivés ou tertiaires. */
val TextTertiary = Color(0xFF808080)

// --- FONCTIONNEL ---
val ErrorRed = Color(0xFFCF6679)
val SuccessGreen = Color(0xFF4CAF50)
